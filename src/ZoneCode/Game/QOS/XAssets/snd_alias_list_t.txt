// =========================================
// snd_alias_list_t
// =========================================
use snd_alias_list_t;
set block XFILE_BLOCK_TEMP;
set string name;
set reusable head;
set count head count;

// snd_alias_t
use snd_alias_t;
set string aliasName;
set string subtitle;
set string secondaryAliasName;
set string chainAliasName;
set reusable soundFile;

// SoundFile
use SoundFile;
set string dir;
set string name;
set condition u::primeSnd type == SAT_PRIMED;
set reusable u::primeSnd;

// MssSound
use MssSound;
set arraysize data info::data_len;
set condition info::data_ptr never;
set condition info::initial_ptr never;

// QosPrimedSound
use QosPrimedSound;
set string name;
set count buffer size;
set block buffer XFILE_BLOCK_LARGE;
